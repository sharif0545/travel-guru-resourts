import React from 'react';

const NotFound = () => {
    return (
        <div style={{textAlign: 'center', marginTop: '100px'}}>
            <h3 style={{color: 'red'}}>404 Error!</h3>
            <h5>Sorry this page is not available.</h5>
        </div>
    );
};

export default NotFound;